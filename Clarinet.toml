[project]
name = "pioneering-chainlink-list"
authors = ["ChainLink Learning Team"]
description = "A blockchain-based learning progression tracker leveraging Chainlink infrastructure"
telemetry = false
requirements = []
[contracts.chainlink-learning-tracker]
path = "contracts/chainlink-learning-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
